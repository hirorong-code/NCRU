# 🌌 Dynamic Construction of Spacetime via NCRUs and Redefinition of Gravitational Fields based on Variational Principles
**Fundamental Formulation: Computational Reconstruction of General Relativity**

## 1. Introduction
This paper defines spacetime not as a geometric manifold (descriptive paradigm), but as a dynamic network of discrete **Minimal Computational Resource Units (NCRUs)** (substantive paradigm). We derive the geometric properties presented by General Relativity as statistical consequences of physical computational processes, returning physics to a realistic formulation independent of geometry.

## 2. Fundamental Physical Processes and the Definition of Proper Time
The base structure of the universe consists of interacting NCRUs maintaining a finite computational bandwidth.

* **Proper Time ($d\tau$)**: Defined physically as the cumulative count of internal state updates (Self-Updates) in a local NCRU.
  $$d\tau = \lim_{N \to \infty} \sum_{k=1}^{N} \delta t_{update}^{(k)}$$
* **Reference Scale ($dt_{sys}$)**: A gauge degree of freedom for descriptive purposes, not a physical observable. All observable physical quantities depend only on ratios such as $d\tau_1 / d\tau_2$ (gauge invariants), naturally ensuring general covariance.



## 3. Derivation of Gravitational Fields via Variational Principles (The Core)
The gravitational field equations are derived from the variational principle that minimizes the **Computational Action** ($S_{comp}$) of the entire network.

$$S_{comp} = \int \mathcal{L}_{NCRU} (\mathcal{S}, \nabla \mathcal{S}) \sqrt{-g} \, d^4x$$

The variation $\delta S_{comp} = 0$ forms a dynamic equilibrium state that coincides with Einstein's field equations in the macro-limit.
$$\nabla^\alpha \left( \mathbb{M} [ \mathcal{S}_{\mu\nu} ] \right) = \chi \cdot T_{\mu\nu}$$

* **$T_{\mu\nu}$ (Task Density)**: Computational density as physical load (Energy-Momentum).
* **$\mathbb{M} [ \mathcal{S}_{\mu\nu} ]$**: A tensor mapping from throughput gradients to metrics.

## 4. Dynamic Derivation of the Lorentz Signature
The signature of the metric $g_{\mu\nu}$ arises from the "exclusivity" in NCRU resource allocation.

* **$g_{00} \propto - |d\tau / dt_{sys}|^2**: Causal constraint due to self-updates (negative metric projection).
* **$g_{ii} \propto 1 / \mathcal{S}_i^2**: Propagation cost of inter-unit communication (positive metric projection).

## 5. Conclusion
Gravity is not the "curvature of space," but a phenomenon where high-load physical processes ($T_{\mu\nu}$) attenuate local throughput ($\mathcal{S}$), causing changes in the path of minimal computational steps (geodesics). The singularity problem is resolved as the saturation of computational resources.